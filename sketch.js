function setup() {
  createCanvas(720, 480);
}

function draw() {
  background(1, 130, 129);

  rectMode(CENTER);
  fill(200, 0, 0);
  rect(360, 200, 40, 30)

  fill(0, 0, 200);
  rect(360, 200, 240, 180)
}
